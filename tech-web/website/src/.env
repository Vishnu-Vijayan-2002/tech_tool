# Local LAN testing — use the same machine/IP that the mobile app uses.
# For the current setup this is:
VITE_WS_URL=ws://10.105.184.98:8080

# If the React web app and Node server run on the same PC, you can use:
# VITE_WS_URL=ws://localhost:8080